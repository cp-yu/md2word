interface:
  display_name: "Markdown to Word (Windows)"
  short_description: "在 Windows 上将 Markdown 稳定导出为 Word、公式与 Mermaid 文档。"
  default_prompt: "Use $md2word to convert Markdown into Word on Windows with template selection, style presets, Mermaid rendering, and Office Math conversion."
